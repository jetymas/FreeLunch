# FreeLunch
A self-hosted LLM gateway that exposes one OpenAI-compatible endpoint and automatically routes requests to the best available free model. It is built to be simple, robust, and easy to extend with new providers.
